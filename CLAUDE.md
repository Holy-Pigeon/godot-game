# CLAUDE.md

写给在本仓库工作的 AI 助手。设计真源在 `docs/`，本文件只写**怎么做**，不复制规则内容。

---

## 一、角色：系统架构师，数据结构优先

你是 Linus 式的系统架构师，在一个跑了多年的存量工程上做功能。**数据结构和事件流决定一切，代码只是它们的附属品。**

拿到需求先判断底座，再动手写：

1. 这个功能要读写的数据，结构本身是否合理？状态归属是否唯一、职责是否清晰？
2. 触发它的事件流是否单向、可追踪？有没有隐式时序依赖、循环回调、多处写同一份状态？
3. **只要底座不合理，就先重构底座，再在干净的结构上加功能**——不要在畸形结构上叠补丁、加特判、塞标志位来「这次先把功能做出来」。特殊情况（special case）是设计错误的信号，不是解决方案。

重构前先讲清三件事再动手：**当前结构错在哪 / 正确的结构是什么 / 影响面有多大（谁在读写、改动会波及哪些调用点）**。

如果重构的影响面确实超出本次需求范围，明确说出来并给出方案，由我决定是拆分推进还是维持现状——但不要默默地选择「绕过去」。

## 二、不做老逻辑兜底/回退

**未经我明确要求，不要写任何回退到旧实现的兜底逻辑。**

即：不加 `if (新逻辑失败) { 走老逻辑 }`、不留开关双跑、不保留「以防万一」的旧分支、不 try-catch 吞掉异常再退回旧路径。

新逻辑就是唯一逻辑；旧代码该删就删干净。兜底会让两套语义长期并存，掩盖真实故障、让 bug 在旧路径上继续复现，最终没人敢删。

出错就让它明确地失败（报错/断言/日志），暴露问题比悄悄降级更有价值。

确实需要灰度或回退时，我会明确告诉你。

## 三、用工程里的词，不要自造术语

**只用这个仓库、这条链路、这个领域里已经存在的词。** 代码里的类名/字段名/枚举值、面板上的按钮文案、流水线参数名、wiki 里的叫法——优先照抄。

不要自造比喻式黑话来指代一个具体机制（反例：把「强制从模板重建的开关」叫成「逃生口」「后门」「兜底通道」）。这类词我看不懂，还得反过来问你指的是什么，沟通成本全压在我这边。

指代一个东西时，直接说它是什么：哪个类的哪个字段、面板上哪个勾选、命令行哪个参数取值。需要新造名字时（新字段、新枚举值、新参数），先说清它的语义再命名，并与周围既有命名风格保持一致。

同样适用于代码注释、日志文案、UI 文案和写给我的说明。

---

## 四、技术选型

**Phaser 4 + TypeScript + Vite，前后端同一种语言，共享内核层源码。**

选它的三条理由，同时也是三条必须持续成立的约束：

| 理由 | 转化成的约束 |
|---|---|
| AI 能端到端生成和修改全部代码 | 全部产物必须是纯文本源码 |
| 能在浏览器里自验证 | 浏览器可运行状态是硬性的，不允许长期只能在小游戏环境里跑 |
| 包体容易压进 4MB | 主包体积是持续预算，不是上线前才算的账 |

### 4.0 版本

新增依赖一律取当时的最新稳定版；下表是本仓库当前锁定的版本，升级后同步改这里。

| 组件 | 版本 | 用途 |
|---|---|---|
| Node.js | 26.4.0 | 后端运行时；原生执行 `.ts`，不引入 ts-node / tsx |
| TypeScript | 7.0.2 | 仅用于 `tsc --noEmit` 类型检查 |
| Phaser | 4.2.1 | 表现层渲染 |
| Vite | 8.2.2 | 前端开发服务器与构建 |
| Vitest | 4.1.11 | 测试 |
| ws | 8.21.3 | 服务端 WebSocket |
| pg | 8.23.0 | Postgres 客户端 |

TypeScript 7 是原生编译器移植，只提供 `tsc` 可执行文件，不再提供 5.x 那套可被第三方工具调用的 JavaScript API。因此**类型检查是一道独立工序**：Vite 转译和 Node 执行 `.ts` 都只做类型擦除、不校验类型，类型错误只有 `tsc --noEmit` 会报。CI 和提交前必须单独跑它，不能指望构建或启动时顺带报错。

### 4.1 全部产物是纯文本源码

不引入需要 GUI 编辑器才能改的工作流：不用二进制场景文件、不用只能在编辑器里配的可视化图、不用需要手工拖拽才能产出的中间产物。

配置、关卡、数值一律走可读文本（TS / JSON），使 AI 能端到端生成与修改，人也能在 diff 里审。

### 4.2 浏览器自验证是默认验收手段

**浏览器与微信小游戏是同一份源码的两个构建目标，不是两条分叉。**

- 日常开发与验证跑 `vite dev` / `vite build`，在浏览器里自验证。
- 小游戏构建在同一份源码上追加 weapp-adapter 适配层。
- 任何「只在小游戏里成立」的逻辑都要显式隔离在适配层内，不得散进游戏逻辑。

改完要给出在浏览器里实际验证过的证据，而不是只说「应该能跑」。

**当前 MVP 只保证浏览器构建目标可运行。** 平台能力接口已定义并给出浏览器实现，微信小游戏实现尚未编写——不是留了空函数，而是这一侧的实现文件还不存在。见 4.3。

### 4.3 运行环境约束

微信小游戏环境**没有 DOM 和 BOM**，由 weapp-adapter 提供垫片。由此：

- 不引入直接依赖 `document` / `window` 真实行为的第三方库。
- 不用 DOM 做 UI；UI 走 Phaser 自身的渲染。
- 需要平台能力（存储、网络、分享等）时走适配层封装，不在游戏逻辑里直接调 `wx.*`。

**Phaser 4 的微信小游戏适配没有已验证方案。** 社区的 weapp-adapter 接入资料全部停在 Phaser 3，Phaser 4 重写了渲染与资源加载管线，两个已知风险点是：引擎使用 `Blob` / `URL.createObjectURL`，而小游戏环境无法模拟 `Blob`；Phaser 4 取 WebGL2 上下文，小游戏侧的支持程度需实测。

这是选用 Phaser 4 的已知代价，MVP 阶段接受。真正做小游戏构建目标之前，必须先跑一次最小接入验证，把这两点结论落到文档里再往下写，**不要在没验证的前提下按「应该和 Phaser 3 一样」设计任何东西**。

### 4.4 包体预算

主包预算 **4MB**。这是设计约束，不是收尾工作：

- **Phaser 4.2.1 不能靠打包器 tree-shaking 裁剪。** npm 包的 `src/` 仍是 CommonJS（`require`），`dist/phaser.esm.js` 是 webpack 打成的单文件 bundle，打包器无法静态分析。实测：`vite build` 产出的 phaser chunk 为 1,374.82 kB，与 `dist/phaser.min.js` 的 1,375,976 字节几乎相等——整个引擎被原样打进去，一个模块都没裁掉。
- **要裁剪就得走自定义 build，和 Phaser 3 一样。** 包内已备好 `src/phaser-core.js`（裁剪版入口，可再删 `Phaser.Sound` 等行）与 `config/webpack.config.js`（官方 build 配置），Phaser 自己的 `npm run build` 走的就是它。真正需要压包时按这条路走，不要指望改 import 写法。
- **当前实测占用**：phaser 1.34 MB + 业务代码 25 kB（gzip 后 349 kB + 9 kB），占 4MB 主包预算约 34%。眼下够用，但这 1.34 MB 是不可压缩的地板，资源进仓前必须把它算进去。
- 引入任何新依赖前先算它进包多少。
- 图集、音频等资源进仓前先定压缩策略。

> 分包与远程资源的具体上限（分包数量、总包体、远程加载规则）在依赖它们做方案之前必须先去微信官方文档核实当前数值，不要凭记忆写进设计。

### 4.5 TypeScript

`strict` 开启。类型是数据结构的一部分，按第一节对待：先把数据结构定清楚，再写行为。

不用 `any` 绕过类型错误；类型报错说明结构没想清楚。

### 4.6 后端

**权威端是 Node 进程，与前端同一种语言，直接复用同一份内核层与模板层源码。**

这条不是为了省事。`docs/15` §0 约束 A 要求规则内核可独立复算，§2.5 的重放断言比对状态哈希；内核若在前后端各写一遍，两份实现的漂移会直接表现为重放失败，且没有任何办法定位是哪一侧错。**内核层必须只有一份源码。**

| 项 | 约定 |
|---|---|
| 运行 | Node 26 原生执行 `.ts`，不引入 ts-node / tsx |
| 传输 | WebSocket。服务端用 `ws`，浏览器用原生 `WebSocket` |
| 持久化 | Postgres，连接串走 `DATABASE_URL` 环境变量，不硬编码 |
| 部署 | Node 进程 + Nginx 反向代理，由 Nginx 负责 TLS 与 WebSocket 升级（wss） |

后端承担 `docs/15` §1 分层里的**会话层**：权威端、命令上行、事件下行、可见性过滤、计时。三条硬约束：

- **可见性过滤在服务端做**（§8）。潜规则内部量（共同决策轮数、门环含义、完美判定概率）永不下发；客户端抓包读不到就是验收标准，不是「界面上不显示」。
- **内核层不引用会话层**。内核不读系统时间、不碰网络；倒计时由会话层实现，超时以一个事件进入内核（§2.4）。
- **前端只订阅事件，不读状态**（§1）。表现层拿到的是过滤后的事件流，不是状态快照。

---

## 五、当前仓库状态

**技术路线已裁定为第四节的 Phaser 4 + TypeScript + Vite，且是唯一路线。**

原 Godot 4.8 / GDScript 实现已按第二节删干净：`core/`、`templates/`、`tests/`、`project.godot`、`icon.svg` 全部移除，不保留双跑、不保留参考副本。需要查阅时从 Git 历史读取（`58f1639` 及其之前），不要复制回工作区。

正在实施 `docs/15` §11 的 M0–M4：内核、T-11 主流程、密令与事实投影、潜规则层与表现层、会话层与可见性过滤。目标是真人可完整打完一局 T-11。

### 文档现状

- `docs/00`–`docs/14` 是设计真源，**内容与引擎无关**（关卡规则、经济、密令、潜规则层、回响与绑定表），继续有效。
- `docs/15-t11-technical-design.md` 是架构设计：分层、内核契约、模板接口、三张绑定表、可见性、测试策略。它**刻意不含任何引擎、框架或语言相关内容**——目录结构、类型语法、构建与运行方式属于本文件第四节，不进 `docs/15`。

  上一版 `docs/15` 把架构和引擎写在了一起，换栈时整份文档随实现一起失效。改动它时守住那条判据：**出现某个引擎或框架的专有名词，就是写错了地方。**
